/** @type {import('tailwindcss').Config} */
// Tailwind v4 auto-detects content via the @tailwindcss/vite plugin; this file
// is kept for tooling/editor compatibility. Design tokens live as CSS variables
// in src/index.css.
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
};
