{
  "name": "vulnerable-server",
  "version": "1.0.0",
  "private": true,
  "description": "Deliberately vulnerable JWT auth server (authorized security lab). Do not deploy.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "genkeys": "node src/keys/generate.js",
    "start": "node src/keys/generate.js && node src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
